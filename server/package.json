{
    "name": "babylon-server",
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "build": "esbuild --outfile=dist/index.js --bundle lib/babylon.ts"
    },
    "devDependencies": {
        "@babylonjs/core": "^6.43.0",
        "esbuild": "^0.20.0"
    },
    "dependencies": {
    }
}
